# ===========================================
# Supabase 配置
# ===========================================
# Supabase 项目的唯一 URL
# 从 Supabase 仪表板获取：Settings > API > Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase 匿名密钥（用于客户端）
# 从 Supabase 仪表板获取：Settings > API > anon public
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase 服务角色密钥（用于服务器端，具有管理员权限）
# 从 Supabase 仪表板获取：Settings > API > service_role (Keep this secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# 超级管理员凭据
# ===========================================
# 管理员邮箱地址
# 用于系统初始设置的超级管理员账户
ADMIN_EMAIL=admin@yourdomain.com

# 管理员初始密码（请在设置后立即更改）
# 注意：实际生产环境中应使用更强密码
ADMIN_PASSWORD=your-secure-password-here

# ===========================================
# 可选：额外的安全配置
# ===========================================
# JWT 密钥（用于签名和验证用户令牌）
# 可以使用 openssl rand -hex 32 生成随机密钥
JWT_SECRET=your-jwt-secret-key-here

# 加密密钥（用于敏感数据加密）
# 可以使用 openssl rand -hex 32 生成随机密钥
ENCRYPTION_SECRET=your-encryption-secret-here

# 开发模式开关
# 设置为 'development' 启用详细日志，其他值则关闭
NODE_ENV=development

# 数据库连接超时时间（秒）
DB_TIMEOUT=30

# API 请求超时时间（毫秒）
API_TIMEOUT=10000